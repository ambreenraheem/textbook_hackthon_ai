# Chatbot Backend API Configuration
# Copy this file to .env.local and update the URL

# Backend API URL for the chatbot
# Default: http://localhost:8000
REACT_APP_API_URL=http://localhost:8000

# Production example:
# REACT_APP_API_URL=https://api.yourdomain.com

# Note: Environment variables in Docusaurus must be prefixed with REACT_APP_
# They are embedded at build time, not runtime
