---
id: appendix-b-python-guide
title: Appendix B - Python Programming Guide for Robotics
description: Essential Python patterns, libraries, and best practices for robotics development.
sidebar_label: B. Python Guide
sidebar_position: 2
category: language-guide
format: narrative
keywords: [Python, programming, NumPy, rclpy, robotics]
---

# Appendix B: Python Programming Guide for Robotics

Essential Python programming patterns and libraries for robotics applications.

## Python Environment Setup

### Virtual Environments
```bash
python3 -m venv robot_env
source robot_env/bin/activate  # Linux/macOS
# or
robot_env\Scripts\activate  # Windows
```

### Package Management
```bash
pip install numpy scipy matplotlib
pip install opencv-python
pip install torch torchvision  # For deep learning
```

## Essential Libraries for Robotics

### NumPy - Numerical Computing
```python
import numpy as np

# Transformation matrices
rotation_matrix = np.array([
    [np.cos(theta), -np.sin(theta), 0],
    [np.sin(theta),  np.cos(theta), 0],
    [0,              0,              1]
])

# Vector operations
position = np.array([1.0, 2.0, 3.0])
velocity = np.array([0.5, 0.0, -0.3])
```

### ROS 2 Python (rclpy)
```python
import rclpy
from rclpy.node import Node
from geometry_msgs.msg import Twist

class MyRobotNode(Node):
    def __init__(self):
        super().__init__('my_robot_node')
        self.publisher_ = self.create_publisher(Twist, 'cmd_vel', 10)

    def publish_velocity(self, linear_x, angular_z):
        msg = Twist()
        msg.linear.x = linear_x
        msg.angular.z = angular_z
        self.publisher_.publish(msg)
```

### OpenCV - Computer Vision
```python
import cv2
import numpy as np

# Read and process image
image = cv2.imread('robot_view.jpg')
gray = cv2.cvtColor(image, cv2.COLOR_BGR2GRAY)
edges = cv2.Canny(gray, 100, 200)
```

## Common Robotics Patterns

### State Machines
[Content to be authored]

### PID Controllers
[Content to be authored]

### Kalman Filters
[Content to be authored]

## Best Practices

- Use type hints for better code clarity
- Follow PEP 8 style guidelines
- Write unit tests for critical functions
- Document complex algorithms
- Handle exceptions gracefully

[Content to be expanded with more examples and patterns]
